# ── Base de datos ──────────────────────────────────────────
DB_PASSWORD=genera_una_contraseña_segura_32_caracteres

# ── JWT ───────────────────────────────────────────────────
JWT_SECRET=genera_un_secreto_muy_largo_64+_caracteres

# ── Admin (se crea en la primera migración) ──────────────
ADMIN_PASSWORD=cambia_esta_contraseña_admin

# ── CORS (dominio del panel admin o frontend) ────────────
CORS_ORIGIN=https://dominiodelapp.com/Perla

# ── Puerto del API (solo para redes internas) ────────────
API_PORT=3000
